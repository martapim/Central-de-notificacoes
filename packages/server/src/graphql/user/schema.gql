input UserInput {
  name: String
  email: String
  password: String
  role: String
}

type Query {
  users: [IUser]
  user(id: String): IUser
}

type Mutation {
  createUser(data: UserInput): Boolean
  deleteUser(id: String): Boolean
  updateUser(data: UserInput, id: String): Boolean
}
